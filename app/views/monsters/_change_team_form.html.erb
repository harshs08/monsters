<% if @teams.present? %>
  <%= form_for [@user, @monster], remote: true do |f| %>
    <%= f.label :teams %>
    <%= f.collection_select(:team_id, @teams, :id, :name) %>
    <%= f.button :submit %>
  <% end %>
<% else %>
  <%= link_to 'Create Team', new_user_team_path %>
<% end %>

 
